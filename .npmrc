# Takram three-geospatial packages declare React 19 + react-three-fiber as peer deps
# (only needed for their optional /r3f bindings, which OrbitWatch never imports — we use
# the vanilla imperative three.js entry points only). Keep legacy resolution so installs
# stay reproducible on React 18.3 without ERESOLVE failures.
legacy-peer-deps=true
